import shoppingcart.Main._
import org.scalatest.FlatSpec

class SetSpec extends FlatSpec{
  "totalPrice method" should "calculate the price of the bulk and singleton carts" in {
    //assert(totalPrice(20, 20) = 40)
  }

  "singletonPrice method" should "calculate the price of the singleton cart" in {
    //assert(singletonPrice() = )
  }

  "bundlePrice method" should "calculate the price of the bundle cart" in {
    //assert(bundlePrice() = )
  }

  "singleton method" should "calculate the number of singleton units where bulk discounts do not apply" in {
    //assert(singleton() = )
  }

  "bundle method" should "calculate the number of bundles where bulk discounts apply" in {
    //assert(bundle() = )
  }
}
}
}